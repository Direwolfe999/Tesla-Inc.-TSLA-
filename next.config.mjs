/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: isProd ? "/Crypgo" : "",
  // assetPrefix: isProd ? "/Crypgo/" : "",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
